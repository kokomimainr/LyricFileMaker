export enum ROUTES {
  HOME = "/",
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  CONSTRUCTOR = "/constructor",
  WORKSPACE = "/workspace",
  LOGOUT = "/logout",
  PROFILE = "/profile",
  RESET = "/reset-password/:token",
  ERROR = '*'
}
